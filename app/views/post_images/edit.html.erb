<h1>編集画面</h1>
<!-- form_with部分 -->
<%= form_with model: @post_image, local: true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :caption %>
  <%= f.submit '保存' %>
<% end %>

<%= link_to "戻る", post_image_path %>